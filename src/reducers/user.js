const initialState = {
  pickerCard: null,
  guid: null,
  name: null,
  isReady: false,
  offline: false,
};

export default (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};
